{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @cofre/web build",
  "outputDirectory": ".next",
  "regions": ["gru1"],
  "crons": [
    {
      "path": "/api/cron/nfse-schedules",
      "schedule": "0 8 * * *"
    }
  ]
}
